# Primal App — Описание поведения приложения

## Общая концепция

Primal App — мобильное приложение-компаньон для настольной кооперативной игры «Primal. Пробуждение». Приложение автоматизирует учёт здоровья монстра, накопленного урона, ярости, фаз боя и смены стоек.

**Платформа:** Kotlin Multiplatform (Android + iOS).

**Состояние приложения управляется конечным автоматом** из состояний `FightPhase`:
`PRE_BATTLE`, `SETUP`, `PHASE_I`…`PHASE_IX`, `VICTORY`, `DEFEAT`.

```
                    ┌─────────────┐
                    │  PRE_BATTLE │──── Начало боя ────┐
                    └─────────────┘                    │
                           ▲                           ▼
                           │                    ┌──────────┐
                           │                    │ PHASE_I  │
                           │                    └────┬─────┘
                           │               ┌─────────┼──────────┐
                           │               │  нанесён урон       │
                           │               ▼                     ▼
                           │        ┌──────────┐          ┌──────────┐
                           │        │ PHASE_II │          │  DEFEAT  │ ← раунды истекли
                           │        └────┬─────┘          └──────────┘
                           │    ┌────────┼────────┐
                           │    │ нанесён урон      │
                           │    ▼                   ▼
                           │ ┌──────────┐    ┌──────────┐
                           │ │PHASE_III │    │  DEFEAT  │
                           │ └────┬─────┘    └──────────┘
                           │      │ … (цепочка до PHASE_IX)
                           │      ▼
                           │ ┌──────────┐
                           └─│ VICTORY  │
                             └──────────┘
```

**Поток данных:** `Monster + Hunter (модели)` → `BattleViewModel (MVVM)` → `StateFlow<BattleScreenState>` → `MainActivity (Compose UI)`

---

## 1. Экран «Подготовка к бою» (экспедиция)

**Файл:** `MainActivity.kt` — функция `QuickBattleHost` (ветка `PRE_BATTLE`/`SETUP`). Неиспользуемые `PreBattleScreen`/`SetupScreen` удалены (defects.md D-17).

Поля ввода (значения по умолчанию):
| Поле | По умолчанию | Описание |
|------|-------------|----------|
| Босс | «Ввести данные вручную» | Выбор из базы боссов предзаполняет поля ниже данными стойки I |
| Сложность | `0` | 0–3 |
| Количество охотников | `4` | Любое целое > 0, верхней границы нет (D-12; решение по числу охотников — qa 94). Текст поля хранится в `CampaignUiState.preBattleHunterCountText` |
| Урон для нанесения раны на игрока | `4` | На игрока. Фактическая прочность = `hunterCount × damageForWound` |
| Здоровье для смены стойки | `7` | `healthForStanceChange` — при каком HP сменить фазу |

Кнопка **«Начать бой»** вызывает `CampaignViewModel.onConfirmQuickBattleStart()`: при некорректном числе охотников (не число или ≤ 0) бой не начинается, иначе вызывается `BattleViewModel.startBattleWithHunters(...)`:
- Создаёт список охотников `Hunter(name = "Охотник $i")`
- Создаёт `Monster(currentHealth = 10, damageForWound = hunterCount × damageForWound, …)` — прочность и порог берутся **из полей подготовки**, даже если выбран босс: правка предзаполненных значений не теряется (D-2)
- Переводит фазу в `PHASE_I`

---

## 2. Экран «Battle» — Боевой экран

**Файл:** `MainActivity.kt:158–306` — функция `BattleScreen`

Отображается при состояниях `PHASE_I`…`PHASE_IX`.

### 2.1 Информационная панель

В верхней части экрана:
- **Текущая фаза** (I…IX) и раунд (N / 10)
- **Здоровье монстра** и текущая **ярость**
- **Накопленный урон** и **прочность** (урон для раны)
- **Статус** («Затвердевший», «Устойчивость стойки», оба через запятую или «Обычный») и **порог смены стойки**
- **Подсветка изменённых параметров (задача 37.1):** после изменения любого параметра (фаза, раунд, здоровье, ярость, накопленный урон, прочность, статус, смена стойки) значение отображается красным (`MaterialTheme.colorScheme.error`) и жирным в течение **1 секунды**, затем возвращается к обычному виду (`BattleScreenState.highlightedParams`, авто-сброс в `BattleViewModel` через `delay(1000)`; повторное изменение продлевает подсветку).
- **Вибрация (задача 37.2):** короткий отклик при нажатии кнопок/управлений и при применении урона (в т.ч. авточерез 2 с после быстрой кнопки); **двойной** отклик при нанесении раны (`woundsInflicted > 0`, заменяет короткий). Ввод символов в поле «Ввести урон» не вибрирует. Android: `Vibrator` + разрешение `VIBRATE`; события эмитятся в `BattleViewModel.vibrationEvents` (`SHORT`/`DOUBLE`).

### 2.2 Нанесение урона

Нанесение урона управляется конечным автоматом режимов ввода (`InputMode`):

```
                    нажатие (+N)
     ┌──── NONE ──────────────────► QUICK_BUTTON ─────┐
     │                                  │   таймер 2с  │
     │  OK / Cancel                клик в поле         │
     │                                  │   (сброс     │
     │                                  ▼   таймера)   │
     ├──────── MANUAL ◄─────────────────┘              │
     │            │                                    │
     │      нажатие (+N)                               │
     │      (накопление,                               │
     │       без таймера)                              │
     │            │                                    │
     └────────────┴── Enter / OK ──► commitDamage() ───┘
```

#### Режимы ввода (`InputMode`)

| Режим | Описание | Таймер |
|-------|----------|--------|
| `NONE` | Начальное состояние, нет активного ввода | Нет |
| `MANUAL` | Пользователь печатает в поле ввода | Нет |
| `QUICK_BUTTON` | Нажаты кнопки (+1/+5/+10/+50) | 2 секунды |

#### Кнопки быстрого добавления

| Кнопка | Значение | Метод ViewModel |
|--------|----------|-----------------|
| +1 | 1 | `onQuickButtonPress(1)` |
| +5 | 5 | `onQuickButtonPress(5)` |
| +10 | 10 | `onQuickButtonPress(10)` |
| +50 | 50 | `onQuickButtonPress(50)` |

**Поведение кнопок (+1/+5/+10/+50):**

1. **Если режим `NONE` или `QUICK_BUTTON`:** значение накапливается (`pendingDamage += amount`), таймер 2с запускается или сбрасывается. Поле ввода обновляется.
2. **Если режим `MANUAL` (пользователь уже печатал):** значение накапливается, но таймер **не запускается**. Поле ввода обновляется.
3. **Таймер 2 секунды:** если в течение 2 секунд не нажата ни одна кнопка, урон применяется автоматически (`commitDamage()`).
4. **Кнопка «Применить урон сейчас (N)»:** отображается при активном таймере для немедленного применения.

#### Ручной ввод урона

- **Поле ввода** (`OutlinedTextField`) управляется ViewModel через `state.damageInputText`.
- При вводе с клавиатуры режим переключается в `MANUAL`, таймер отменяется.
- **Клавиша Enter** на клавиатуре или кнопка **OK** — мгновенное применение урона.
- **Кнопка «Отмена»** — сброс `pendingDamage` в 0, очистка поля, возврат в `NONE`.

#### Заживление раны и отрицательный урон

- Кнопка **«Заживить рану (+1 здоровья)»** (`BattleViewModel.healWound()` → `Monster.healWound()`): +1 здоровья по правилу «заживить рану», жетоны урона (`accumulatedDamage`) не меняются. Здоровье не поднимается выше начального (10); у побеждённого монстра не действует. Действие отменяется «Отменить действие» (`ActionType.HEAL`) (defects.md R-4).
- **Отрицательный урон** в поле ввода уменьшает только накопленный урон: `accumulatedDamage -= min(|amount|, accumulatedDamage)`. Здоровье не меняется; при `accumulatedDamage = 0` ввод ничего не делает — сообщение «Накопленного урона нет.» (D-3). Под полем «Ввести урон» выводится подсказка «Отрицательное значение отменяет накопленный урон». Клавиатура поля — `KeyboardType.Decimal`, знак минуса доступен.

#### Смешанные сценарии

| Сценарий | Поведение |
|----------|-----------|
| Кнопки (+N) → клик в поле ввода | Таймер останавливается, урон только по OK/Enter |
| Ручной ввод → кнопки (+N) | Число меняется, таймер не запускается |
| После Отмена / OK | Возврат в `NONE`, таймер запустится при следующем (+N) |

#### Применение урона

**Файл:** `BattleViewModel.kt` — метод `commitDamage`

Вызывает `Monster.takeDamage(pendingDamage)`. Перед применением сохраняет снимок состояния монстра (`MonsterSnapshot`) для возможной отмены.

#### Отмена последнего действия

**Файл:** `BattleViewModel.kt` — метод `onUndoPress`

Кнопка **«Отменить действие»** отображается после каждого мутирующего действия (`canUndo = true`). Поддерживается **до 10 отмен** подряд. Восстанавливает полное состояние монстра (здоровье, фазу, накопленный урон, ярость, прочность, порог стойки), `FightPhase` и **номер раунда** (`ActionSnapshot.round`, D-4) на момент перед последним действием. Отмена урона, вызвавшего автоматическую смену стойки по данным из базы боссов, возвращает и прежнюю стойку.

**Действия, которые можно отменить:**
- Нанесение урона и отрицательный урон (`DAMAGE`)
- Заживление раны (`HEAL`)
- Изменение ярости через кнопки ±1, +1/охот, +1/охот-1 (`RAGE`)
- Смена стойки через диалог или кнопку «Сменить стойку» (`PHASE_CHANGE`)
- Завершение раунда (`ROUND_END`) — раунд возвращается к прежнему номеру

**Реализация:** стек `actionHistory: MutableList<ActionSnapshot>` (max 10). Снимок сохраняется перед каждым действием через `saveSnapshot()`. При отмене:
1. Извлекается последний `ActionSnapshot`
2. Восстанавливаются поля `Monster` (health, accumDamage, phase, rage, isDefeated)
3. Восстанавливается `FightPhase`
4. `canUndo = actionHistory.isNotEmpty()`

Алгоритм `Monster.takeDamage` (файл: `MonsterExt.kt`):
1. Отрицательное значение — уменьшение накопленного урона (см. выше), раны не наносятся.
2. `accumulatedDamage += amount`
3. **Цикл ран:** пока `accumulatedDamage >= damageForWound`:
   - `accumulatedDamage -= damageForWound`
   - `currentHealth -= 1`
   - Если `currentHealth <= 0` → `isDefeated = true`, возврат
   - Если `currentHealth <= healthForStanceChange` и не последняя фаза → `currentPhase++`, **цикл останавливается**: оставшийся урон переносится на новую стойку и наносится уже с её прочностью (правила, стр. 1440–1493; defects.md R-2)
4. **После цикла:** если «Затвердевший» и были раны → `accumulatedDamage = 0` (остаток сгорает). Иначе остаток сохраняется

**Смена стойки после урона** (`BattleViewModel.applyDamage`, используется в `commitDamage`, `endRound`, `confirmPhaseChange`):
- при каждой смене стойки открывается диалог «Смена стойки!» (§3); перенесённый урон ждёт подтверждения параметров новой стойки;
- если стойка известна из базы боссов (`selectedBoss.getStance(phase − 1)`), поля диалога заполнены её значениями, их можно исправить (`BattleScreenState.phaseChangeFromBossData = true`); иначе поля пустые;
- победа определяется двумя проверками: `monster.isDefeated` **или** сообщение «побеждён» (D-18).

Пример книги: вираксен (прочность 4, порог 7, здоровье 9), 13 урона → 2 раны, 5 урона переносится на стойку II (прочность 6) — третьей раны нет.

### 2.3 Управление яростью

**Файл:** `BattleViewModel.kt:159–199`

В начале боя монстр получает **ярость, равную числу охотников** (`startBattleWithHunters` → `rage = hunters.size`; задача 35.2).

| Кнопка | Метод | Поведение |
|--------|-------|-----------|
| -1 | `removeRage(1)` | `rage = max(rage - 1, 0)` |
| +1 | `addRage(1)` | `rage += 1` |
| +1/охот | `addRagePerHunter()` | `rage += hunterCount` |
| +1/охот-1 | `addRagePerHunterMinusOne()` | `rage += max(hunterCount - 1, 0)` |

### 2.4 Статусы «Затвердевший» и «Устойчивость стойки»

Два независимых переключателя (defects.md R-3), у каждого — кнопка **«i»** в сером кружке, открывающая описание статуса (`MonsterStatusInfo`, `BattleViewModel.onStatusInfoRequested/onStatusInfoDismissed`, диалог с кнопкой «Понятно»):

| Статус | Метод | Поле | Действие |
|--------|-------|------|----------|
| «Затвердевший» (состояние монстра, например торамат и дигоракс) | `toggleHardened()` | `Monster.isHardened` | После нанесения ран остаток урона **сгорает** (`takeDamage`) |
| «Устойчивость стойки» (ключевое слово карты стойки) | `toggleResilient()` | `Monster.isResilient` | При смене стойки накопленный урон **не переносится** на новую стойку (`resetPhase` обнуляет `accumulatedDamage`) |

### 2.5 Выплеск ярости

**Файл:** `BattleViewModel.kt` — метод `confirmRageSurge`, поле `showRageSurgeDialog`

При каждом изменении ярости (кнопки +1/-1/+охот, завершение раунда, отмена действия) проверяется условие:
```
rage >= hunterCount * 3
```

При срабатывании:
- Отображается диалог **«Выплеск ярости»** (термин правил): «Ярость монстра достигла 3 за каждого охотника. Каждый охотник получает урон, равный силе монстра. После этого ярость сбрасывается до 1 за каждого охотника» (defects.md R-8)
- Кнопка **OK** вызывает `confirmRageSurge()` — ярость сбрасывается до `hunterCount`, сообщение «Выплеск ярости! Каждый охотник получил урон, равный силе монстра. Ярость сброшена до N»

### 2.6 Выход в главное меню

Кнопка **«Выход в меню»** — доступна на боевом экране, экране победы и поражения:
- **QuickBattle, бой идёт:** `campaignViewModel.onPauseBattle()` — бой сохраняется («Вернуться к бою»)
- **QuickBattle, экраны победы и поражения:** `viewModel.resetBattle()` + `campaignViewModel.onBackToMenu()` — сброс боя и возврат в главное меню
- **CampaignBattle, бой идёт:** `campaignViewModel.onPauseBattle()` — бой сохраняется («Вернуться к бою»)
- **CampaignBattle, экран победы:** `campaignViewModel.onVictoryExitToMenu()` — результат боя не теряется: применяются награды задания (в прологе — трофей Вираксена и 2 стихии) и награды главы, затем главное меню (D-13)

### 2.7 Завершение раунда

**Файл:** `BattleViewModel.kt` — метод `endRound`

Кнопка **«Закончить раунд»**:
1. Если есть накопленный, но не применённый урон (`pendingDamage > 0`):
   - Вызывает `Monster.takeDamage(pendingDamage)`, получает `DamageResult`
   - Если босс **побеждён** → `phase = VICTORY`, досрочный выход (раунд не завершается)
   - Если **смена стойки** → обновляется `FightPhase`, открывается диалог «Смена стойки!» (поля — из базы боссов, если стойка известна)
2. Вызывает `Monster.endRound(hunterCount)` — добавляет +1 ярости за каждого охотника
3. `currentRound += 1`
4. Если `currentRound > maxRounds (10)` → `phase = DEFEAT` (поражение)
5. Иначе продолжается текущая фаза, проверяется всплеск ярости

---

## 3. Смена стойки — диалог PhaseChangeDialog

**Файл:** `MainActivity.kt:308–352`

Отображается при `state.showPhaseChangeDialog == true` (модальное окно `AlertDialog`) при каждой смене стойки — по порогу здоровья или по кнопке «Сменить стойку».

Информация в окне:
- «Монстр перешёл на стойку N.»
- откуда значения: «Параметры заполнены из базы боссов — при необходимости исправьте:» (стойка есть у выбранного босса; значения — `BattleScreenState.pendingDamageForWound`/`pendingHealthForStanceChange` из `selectedBoss.getStance(N − 1)`) или «Данных о стойке в базе боссов нет — укажите параметры:» (ручные данные или стойка за пределами карт босса — поля пустые);
- если есть перенесённый урон: «Перенесённый урон K будет нанесён с новой прочностью после «OK».», а при «Устойчивости стойки» — «Перенесённый урон K сбросится («Устойчивость стойки»)».

Три поля:
- **Урон для нанесения раны на игрока** — новый `damageForWound` (на игрока, умножается на `hunterCount`)
- **Здоровье для смены стойки** — новый порог. Если `> 0`, появляется третье поле. Если `0` или пусто — последняя стойка (дальнейшая смена не происходит).
- **Здоровье босса в новой стойке** — опционально, начальное HP для новой фазы

Кнопка **OK** вызывает `viewModel.confirmPhaseChange(wound, stance, bossHealth)`:
- `Monster.resetPhase(damageForWound, healthForStanceChange)` — обновляются пороги; `accumulatedDamage` переносится на новую стойку, обнуляется только при «Устойчивости стойки»
- Если `bossHealth > 0`, устанавливается `currentHealth = bossHealth`
- Перенесённый урон сразу наносится с новой прочностью (R-2); если он снова доводит до порога, окно сразу показывает следующую стойку (номер, перенесённый урон и поля обновляются)
- Диалог закрывается. При следующем достижении порога смены стойки диалог появится снова — до 9 стоек.

Кнопка **Отмена** вызывает `viewModel.dismissPhaseChangeDialog()`:
- Откатывает урон, вызвавший смену стойки (через `onUndoPress()`)
- Восстанавливает состояние монстра к моменту перед уроном
- Закрывает диалог (`showPhaseChangeDialog = false`)

---

## 4. Экран победы

**Файл:** `MainActivity.kt:354–369` — функция `VictoryScreen`

Отображается при `FightPhase.VICTORY`.

Содержимое: «ПОБЕДА! Монстр повержен!» + кнопка **«Новый бой»** (`viewModel.resetBattle()`).

---

## 5. Экран поражения

**Файл:** `MainActivity.kt:371–386` — функция `DefeatScreen`

Отображается при `FightPhase.DEFEAT` (истекли 10 раундов или «Сдаться»).

Содержимое: «ПОРАЖЕНИЕ!» и причина — «Закончились раунды...» либо «Вы сдались.», если поражение наступило из-за «Сдаться» (`BattleScreenState.surrendered`, функция `defeatReason`; D-14) — плюс кнопка **«Новый бой»**. То же на экране поражения в кампании.

---

## 6. Сброс боя

**Файл:** `BattleViewModel.kt:237–240` — метод `resetBattle`

Отменяет таймер (`timerJob?.cancel()`) и сбрасывает `_state` в `BattleScreenState()` (все значения по умолчанию, фаза `PRE_BATTLE`).

---

## 7. Режим кампании (v2 — реализовано)

- До **10 кампаний** с локальным хранением через Room (SQLite) в KMP-модуле shared
- Создание кампании: название + выбор классов охотников (8 классов)
- Лист кампании: охотники, навыки, ресурсы, заметки, глава
- **Древо навыков**: 5 ветвей (А–Д) × 2 ступени, tier 2 требует tier 1. Состояние кнопки навыка передаётся не только цветом, но и семантикой Compose: `selected = true/false` и `stateDescription` «Открыт»/«Закрыт» (`CampaignSheetScreen.SkillCheckbox`) — доступно TalkBack и UI-тестам (задача 42.2). Для кнопок (роль не Tab) Compose отображает `selected` в `checkable = true` / `checked`, поэтому в UiAutomator2 состояние читается из атрибута `checked`; атрибут `selected` остаётся `false`. Внешний вид кнопок не изменился.
- **Ресурсы**: 6 материй, 6 растений, 9 стихий — учёт, кнопки +/− (по 1, минимум 0). Рядом с каждым ресурсом отображается его иконка из `androidApp/src/main/res/drawable/` (файлы `scales.png`…`ice.png`, маппинг `Material/Plant/Element.iconRes()`; задача 35.3/35.4).
- **Продвижение**: переход к следующей главе (глава += 1) происходит при «Принять» наград главы после победы; ручной режим — кнопки на листе кампании. Глава ограничена диапазоном **1–12** (`CampaignViewModel.MAX_CHAPTER`; `onUpdateChapter` — `coerceIn(1, 12)`, переход после наград — не выше 12; D-11).
- **Нумерация глав** (defects.md R-1): глава приложения = глава книги + 1 (пролог — глава приложения 1). Условия заданий по главе (`CHAPTER_IN`, `ACHIEVEMENT_OWNED_IN_CHAPTER`) сравнивают **главу книги** `bookChapter(currentChapter) = currentChapter − 1`: например, после задания 1 задание 4 открывается в главах книги 1–2 (главы приложения 2–3).
- **Кузня/лаборатория**: старт 1 → гл.4 → 2 → гл.8 → 3 (повышение при «Принять» наград главы с флагами `forgeUpgrade`/`labUpgrade`)
- **Пост-победный флоу**: трофей + завершение активного задания + выбор открывшихся заданий + ввод ресурсов (материи и растения, начисляются всем охотникам) + стихия босса
- **Награда за победу над боссом**: каждому охотнику начисляется **2** стихии поверженного босса (в `onConfirmVictory`); для боссов без стихии («Пробуждённый») стихия не начисляется.
- **Предзаполнение пролога**: при старте новой кампании (`onStartCampaign` → `startBattleInternal`, `isPrologue = true`) в меню подготовки автоматически выбран босс **Вираксен** сложности **0** (поля dfw/hsc заполняются из первой стойки Вираксена сложности 0). Для последующих боёв кампании предзаполнение не применяется.
- **Идемпотентность заданий**: `quests` имеет уникальный индекс `(campaign_id, quest_id)` (миграция 8→9 чистит существующие дубликаты); `saveQuest` — апсерт (`QuestDao.upsertQuest`: `INSERT OR IGNORE` + `UPDATE` в одной транзакции; синтаксис `ON CONFLICT … DO UPDATE` не используется — он требует SQLite 3.24+, а на Android 8–10 при minSdk 26 SQLite 3.18–3.22), повторные нажатия «Продолжить»/«К листу кампании» не создают дубликатов.
- **Начисление ресурсов реальным охотникам**: после создания кампании (`onStartCampaign`) в `state.hunters` кладутся охотники с реальными id из БД (перечитываются через `getHunters(campaignId)`), а не с `id = 0`. Пост-победные ресурсы начисляются по этим id.
- **Защита от двойного сохранения**: `onConfirmVictory`/`onQuestRewardsAccept` блокируют повторный вызов (`isSaving`), кнопки «Продолжить»/«Принять» неактивны на время сохранения; после успешного сохранения закрывается текущий диалог и открывается окно наград главы (`showChapterRewards`), а переход на лист кампании происходит после «Принять»/«Отклонить» в нём.
- **Автосохранение** при победе/поражении/каждом раунде
- **Флоу заданий**: перед боем выбирается активное задание из открытых (диалог «Выбор задания»); при победе оно завершается. После победы вводятся открывшиеся задания (чекбоксы) и ресурсы. После поражения вводится открывшееся задание (чекбоксы), которое становится доступным.
- **Список заданий в листе кампании**: показываются только **открытые и не выполненные** задания (`isAvailable = true && !isCompleted`; задача 42.3 — выполненные задания в листе не отображаются, qa 57), отсортированные по возрастанию `questNumber`. Для каждого задания отображается название и босс (стихия + имя) из каталога `TaskInfo` (например, «Память пустыни — Торамат (Рог)»); при отсутствии данных каталога — «Задание N». То же отображение применяется в диалоге выбора активного задания перед боем (`QuestSelectDialog`). Кнопка «Выполнено» помечает открытое задание выполненным **и открывает зависимые задания** из каталога `TaskInfo` (безусловный список `victoryOpenQuests` + условные правила `victoryOpenQuestConditions` с учётом текущей главы и достижений — например, после задания 1 в главах 1–2 открывается задание 4, иначе 6). Материи/растения/достижения при ручном «Выполнено» не начисляются (только открытие заданий; решение qa 70). Кнопка «Выполнено» (`onCompleteQuest`) — не переключатель: выполненное задание пропадает из списка открытых и из диалога выбора задания, повторно снять отметку нельзя (задача 42.3). То же после победы: «Принять» в окне наград завершает активное задание, и оно исчезает из листа. Выполненные задания показываются серым в отдельном списке **«Выполненные:»** (заголовок жирный) под открытыми (`CampaignUiState.campaignCompletedQuests`). У каждого выполненного задания — кнопка **«Отмена»** (`onUncompleteQuest` → `QuestDao.uncompleteQuest`): задание снова становится открытым и невыполненным; задания, открытые его выполнением, и полученные награды не меняются (qa 119). В диалоге «Редактировать» номера выполненных заданий серые и недоступны (`Checkbox(enabled = false)`); `onToggleEditedQuest` и `onSaveQuestEdits` их игнорируют, а `QuestDao.upsertQuest` не снимает признак выполнения — вернуть выполненное задание в открытые можно только кнопкой «Отмена» (defects.md D-5, qa 119).
- **Достижения в листе кампании**: секция «Достижения» показывает полученные достижения (зелёным) и кнопку «Редактировать». Диалог редактирования позволяет **добавить** достижение (свободный ввод названия) или **удалить** имеющееся. Достижения выдаются за победы (`TaskInfo.victoryAchievements`), решения глав и вручную; все считаются «полученными» (`unlocked = true`). Удаление достижения — DAO `deleteAchievement(campaign_id, achievement_id)`. Одно достижение хранится в кампании один раз: уникальный индекс `(campaign_id, achievement_id)`, повторная вставка пропускается (`OnConflictStrategy.IGNORE`; D-9).
- **Сравнение названий достижений** (задача 42.1): во всех условиях — условные задания и сообщения глав, условия заданий (`ACHIEVEMENT_OWNED`, `ACHIEVEMENT_NOT_OWNED`, `ACHIEVEMENT_OWNED_IN_CHAPTER`), условные достижения — названия сравниваются без учёта регистра, различия «ё/е» и лишних пробелов (`domain/AchievementNames.kt`: `normalizeAchievementName`, `achievementMatches`, `containsAchievement`). Выдача достижения (награды задания и поражения, условные достижения, решение главы) пропускает уже полученное равнозначное достижение — дубли не создаются. При ручном добавлении равнозначное достижение в другом написании заменяется новым написанием. В каталоге заданий названия приведены к написанию условий глав: «Народ Золотых гор» (зад. 5), «Яд Пазиса» (зад. 36), «Копьё драконоборца» (зад. 18 и 45). «Заезда Дракона» из гл. 9 `compainInfo.md` трактуется как «Звезда дракона» (qa 93).
- **Окно наград за задание** (`QuestRewardsDialog`): после победы/поражения в бою кампании (**кроме пролога**) показывается окно с наградами активного задания из каталога `TaskInfo` (материи, растения, открываемые задания, достижения, карты наград, особые награды; при поражении — открываемые задания). Кнопки:
  - Заголовки «Ресурсы:», «Материи:», «Растения:» выделены жирным, как в окне наград главы.
  - **Условные правила задания** (победы или поражения) выводятся под «Открываемые задания» в формулировке правил, под каждым — результат для текущей кампании (раздел «Условные задания:», для условных достижений — «Условные достижения:»). Например: «Если текущая глава 1 или 2, то добавить задание 4, иначе добавить задание 6» → «Добавлено задание 4.»; «Если есть достижение «Голос Волтьяра», добавить достижение «Уробборос»» → «Достижения нет.». У задания 25 второе условие получает результат «Не применяется: уже добавлено задание 34.». Результат считается той же функцией, что применяет награды при «Принять» (`domain/ConditionOutcomes.kt`: `evaluateTaskConditions`), по достижениям до выдачи наград задания и по главе книги (R-1).
  - «Принять» — автоматически применяет награды: начисляет материи/растения всем охотникам, 2 стихии босса, выдает достижения, открывает задания, завершает активное задание, сохраняет трофей и открывает окно наград главы.
  - **Бой без задания** («Продолжить без задания», задача 42.4): окно показывает «Бой без задания» и босса, выбранного на экране подготовки к бою (`selectedPreBattleBoss`). «Принять» сохраняет трофей этого босса, начисляет каждому охотнику 2 его стихии (босс без стихии — Пробуждённый — стихий не даёт) и открывает окно наград главы; задание не завершается. Если босс не выбран (режим «Ввести данные вручную»), награды не применяются и показывается «Босс не выбран — укажите его через «Редактировать»». Ошибки «У задания не указан босс» / «…стихия босса» относятся только к заданиям из каталога.
  - «Редактировать» — для победы открывает `PostVictoryDialog`, предзаполненный данными каталога (босс, стихия, материи, растения, задания); для поражения — форму с чекбоксами номеров заданий (сохранение через `onBattleFinished`).
  - «Выход» — закрывает окно без применения.
- **Окно наград за поражение** показывает открываемые задания, условные задания поражения с результатом (зад. 2: «Если текущая глава 1 или 2, то добавить задание 5») и достижения поражения («Оледенение», зад. 47 и 48) до нажатия «Принять» (D-15). Награды за поражение — рекомендация книги кампании, приложение их применяет (R-7, qa 94).
- Для пролога окно наград задания не показывается: после победы над Вираксеном (первый бой кампании) окно «Задание выполнено!» (`PostVictoryDialog`) также **не** показывается — трофей Вираксена и 2 стихии «Огонь» начисляются каждому охотнику **молча** (`applyPrologueVictory`), после чего сразу открывается окно наград главы (задача 36.1). Изменение действует только для самого первого боя новой кампании; последующие победы и поражения — без изменений (qa 77–80).
- **Флаг окна наград**: `CampaignUiState.showQuestRewards` + `questRewardsMode` (VICTORY/DEFEAT) + `activeQuestNumber`; награды берутся из `taskInfoByQuestNumber`.
- **Окно наград за главу** (`ChapterRewardsDialog`): после победы и утверждения наград задания (в прологе — сразу после победы) показывается окно с наградами/эффектами главы из `ChapterInfo`. Кнопки:
  - «Принять» — применяет эффекты главы: начисляет ресурсы главы всем охотникам, открывает задания (`openQuests` + условные по достижениям), помечает истёкшие недоступными (`expireQuests`), повышает уровни кузни/лаборатории при флагах (`updateForgeLevel`/`updateLabLevel`), переходит к следующей главе (`updateChapter(currentChapter + 1)`) и открывает лист кампании.
  - «Отклонить» — не применяет эффекты главы и не меняет главу; переход к листу кампании.
  - Интерактивные решения (`ChapterDecision`, гл.7 «Тренироваться у подножья Волтьяра» Да/Нет) — кнопки выбора; при выборе варианта `optionForAchievement` выдаётся достижение `achievementOnOption`. Выбор другого варианта или «Отклонить» наград главы отзывает достижение, выданное этим решением; достижение, полученное раньше, не отзывается (D-10).
  - «Улучшение набора охотника» в главах 8 и 10 показывается только при достижении «Голос Волтьяра» (`ChapterInfo.hunterKitUpgradeAchievement`, C-9).
  - Глава 10: задание 30 открывается, если нет **хотя бы одного** из достижений «Три копья» и «Эхо водопада» (C-8).
  - **Финал кампании** (R-6): в главе 11 истекают все открытые задания (`ChapterInfo.expireAllQuests`) и выводится «Истекло время всех заданий. Следующий бой — финальный: Пробуждённый». В главе 12 «Начать бой» сразу открывает подготовку к бою с Пробуждённым (`ChapterInfo.finalBossName` предыдущей главы) без выбора задания. После победы окно наград главы сообщает «Кампания пройдена! Пробуждённый повержен.».
  - **Условия главы** выводятся в формулировке правил с результатом проверки по достижениям кампании (`evaluateChapterConditionalQuests`, `evaluateChapterConditionalRewards`; пересчитываются после решения главы):
    - «Условные задания:» — например, «Если есть достижение «Упавшая звезда», открыть задание 15, иначе добавить задание 47» → «Добавлено задание 47.»;
    - «Условные награды:» — условное улучшение набора охотника («Если есть достижение «Голос Волтьяра» — улучшение набора охотника» → «Достижение есть, улучшите набор охотника.» / «Достижения нет.») и условные сообщения («Если есть достижение «Яд Пазиса»: Получите награду 25» → «Достижение есть: Получите награду 25.» / «Достижения нет.»).
    Безусловные эффекты (улучшение набора без условия, кузня, лаборатория, сообщения главы) выводятся в сообщении вверху окна.
- **Переход глав**: `saveVictory` больше не вызывает `advanceChapter`; переход к следующей главе происходит при «Принять» наград главы (глава += 1). Ручное изменение главы доступно на листе кампании (`onUpdateChapter`).
- **Режим экспедиции**: быстрый бой без кампании. BattleViewModel сохраняется в CampaignViewModel, состояние боя не теряется при выходе в меню и возврате.
- **Pre-battle диалог**: выбор босса из БД (дедупликация по имени + «Ввести данные вручную»), выбор сложности (0-3). Список боссов сортируется: «Ввести данные вручную» первым, затем боссы по алфавиту (сначала стихия по русскому названию, затем имя), «Пробуждённый» последним. Для кампании сложность предзаполняется по главе: 1→0, 2-4→1, 5-8→2, 9-11→3. При выборе босса/сложности поля dfw/hsc автозаполняются из первой стойки выбранной сложности. При выборе босса в кампании после победы поля «стихия» и «название босса» предзаполняются. **Синхронизация босса при старте боя кампании** (задача 42.4): при каждом старте — пролог, бой по заданию, бой без задания — после загрузки боссов `selectedPreBattleBoss` пересчитывается по выбранному имени и сложности главы (`CampaignViewModel.syncPreBattleBoss`). Для задания это босс из каталога заданий; поля dfw/hsc берутся из его стойки I; если у босса одна сложность (Пробуждённый — только 3), берётся она. Раньше это делалось только в прологе, и в боях по заданиям стойки и стихия оставались от предыдущего боя.
- **База боссов**: `BossEntity` — название, стихия (nullable), сложность, до 5 стоек с damageForWound (nullable) и healthForStanceChange. Боссы: Вираксен (Огонь), Иекорос (Молния, смена по запросу), Пробуждённый (без стихии, только сложность 3, 5 стоек), Торамат (Рог), Юром (Металл), Озев (Молния), Моркраас (Кристалл), Дигоракс (Рог), Харджа (Огонь), Коровон (Коралл, стойка II без порога раны), Таррагуа (Металл), Фелаксир (Кристалл), Оруксен (Коралл), Пазис (Перо), Нагарджас (Перо), Зекалит (Молния), Зекат (Молния), Тараск (Огонь), Кситерос (Перо, Ст2 — смена по запросу), Гидар (Яд), Рейкал (Яд), Сиркаадж (Лёд), Мумараак (Лёд). Seed выполняется и при миграции (8→9...12→13), и при создании БД (`onCreate`-колбэк) — на чистой установке список боссов заполнен (23 босса). `seedBosses` заполняет стойки 4–5 значением 0, а `BossEntity.stance4Dfw/stance5Dfw` имеют `@ColumnInfo(defaultValue = "0")` — Room-схема совпадает с `CREATE_BOSSES_TABLE`, onCreate-seed не падает с `NOT NULL constraint failed: bosses.stance4_dfw`.
- **Каталог заданий**: `TaskInfoEntity` (таблица `task_info`, миграции 9→10, 10→11, 11→12, 12→13, 13→14) — статический справочник по 49 заданиям из `doc/taskInfo.md`: номер, название, босс (имя + стихия), награды за победу (материи, растения, открываемые задания, достижения, карты наград, особые награды) и награды за поражение (открываемые задания). Мапы/списки хранятся текстом (`NAME:qty` через `;`, списки номеров через `,`). Seed выполняется при миграции и при создании БД (`onCreate`). Условные правила (`TaskCondition`) хранятся в колонках `victory_open_quest_conditions`/`defeat_open_quest_conditions` текстом: `kind|achievement|chapterSet|quest|else|rewardAchievement`, условия разделяются `;`. Виды условий: `CHAPTER_IN`, `ACHIEVEMENT_OWNED`, `ACHIEVEMENT_NOT_OWNED`; комбинированное «достижение И глава» — `ACHIEVEMENT_OWNED_IN_CHAPTER` (зад. 25); условные достижения (выдача достижения при наличии другого, зад. 29/40) — поле `rewardAchievement`. При применении условий открывается **первое** условие, давшее целевое задание (зад. 25: при главе 8 задание 34 вместо 27). Доступ через репозиторий: `getAllTaskInfo()` / `getTaskInfo(questNumber)`.
- **Каталог глав**: `ChapterInfoEntity` (таблица `chapter_info`, миграции 10→11, 14→15) — справочник по 11 главам из `doc/compainInfo.md`: награды (материи/растения), открываемые и истекающие задания (в т.ч. условные по достижениям; `negated` + `requireAll` — «нет хотя бы одного»), повышение уровней кузни/лаборатории, «улучшение набора охотника» (флаг и необязательное достижение-условие), истечение всех заданий и босс финального боя (гл. 11), интерактивные решения (`ChapterDecision`), сообщения. Ссылки на задания 41/46 оставлены как в источнике. Доступ через репозиторий: `getAllChapterInfo()` / `getChapterInfo(chapter)`.
- **Стойка без порога раны** (`damageForWound = null`): на такой стойке урон только накапливается в `accumulatedDamage`, раны не наносятся и босс не может быть побеждён. При подтверждении смены со стойки без порога на стойку с порогом весь накопленный урон немедленно пересчитывается в раны по прочности новой стойки (например, Коровон: стойка II накапливает урон, при переходе на стойку III урон наносится). Поле «урон для нанесения раны» в pre-battle и в меню смены стойки можно оставить пустым — стойка будет без порога раны (урон накапливается).
- **Смена стойки**: при переходе на новую стойку поля диалога предзаполняются данными из `selectedBoss.stances[currentPhase]`.
- **Боссы с ручной сменой стойки**: `healthForStanceChange = null` — стойка меняется по запросу (кнопка «Сменить стойку», активна при null). Иекорос (Молния) — все стойки с null. В окне «Подготовка к бою» поле «Здоровье для смены стойки» не предзаполняется для таких боссов. При ручном вводе пустое поле hsc допускает старт боя (смена по кнопке).
- **Кнопка «Сдаться»**: в бою с диалогом подтверждения, переход в DEFEAT.
- **Сохранение состояния при повороте**: `android:configChanges` в манифесте предотвращает пересоздание Activity при повороте экрана. Бой не сбрасывается при смене ориентации.
- **Устойчивость к ошибкам БД**: `fallbackToDestructiveMigration(true)` на обеих платформах. Миграции: `1→2` (колонка `quest_number`), `2→3`/`3→4`/`4→5` (боссы, `element` nullable, «Пробуждённый»), `5→6`/`6→7`/`7→8` (пересоздание боссов + seed, `dfw` nullable), `8→9` (уникальный индекс `quests(campaign_id, quest_id)`), `9→10` (таблица `task_info`), `10→11` (колонки условий в `task_info` + таблица `chapter_info`), `11→12` (пере-сид `task_info` с расширенной моделью условий — составное условие зад. 25, условные достижения зад. 29/40), `12→13` (добавление 4 боссов: Гидар, Рейкал — Яд; Сиркаадж, Мумараак — Лёд; пересоздание `bosses`), `13→14` (задача 42.1: пере-сид `task_info` с каноническими названиями достижений; сохранённые достижения кампаний «Яд пазиса», «Народ золотых гор», «Копье драконоборца» переименовываются в канонические; точные дубли достижений удаляются; схема не меняется), `14→15` (решения defects.md: колонки `chapter_info.hunter_kit_upgrade_achievement`, `expire_all_quests`, `final_boss`; пере-сид `chapter_info` и `task_info`), `15→16` (D-9: удаление дублей достижений и уникальный индекс `achievements(campaign_id, achievement_id)`). Seed-функции каталогов вставляют только колонки, существующие в схеме на момент миграции (`SeedSql.kt`: `columnsOf`, `insertExistingColumns`) — ранние миграции `10→11` и `12→13` проходят при обновлении со старых версий.
- **Возврат к активному бою**: при выходе из незавершённого боя (PRE_BATTLE или PHASE_I–IX) в главное меню появляется кнопка «Вернуться к бою». Нажатие восстанавливает экран боя с сохранённым состоянием. При завершении боя (VICTORY/DEFEAT) кнопка не появляется.
  - `CampaignUiState.lastActiveBattle: AppScreen?` — сохраняет экран боя при выходе из активной фазы
  - `CampaignViewModel.onPauseBattle()` — сохраняет текущий экран в `lastActiveBattle`, переводит в `MainMenu`, не уничтожает BattleViewModel
  - `CampaignViewModel.onResumeBattle()` — восстанавливает экран из `lastActiveBattle`

Реализовано в:
- `shared/.../database/` — Room KMP (10 Entity, 10 DAO, mapper, repository impl, platform expect/actual)
- `shared/.../viewmodel/CampaignViewModel.kt` — управление состоянием кампании
- `shared/.../domain/` — валидатор навыков и сравнение названий достижений (неиспользуемые `ResourceExchangeValidator`, `ChapterProgression` и методы репозитория `exchangeResources`/`advanceChapter`/`getHuntersWithResource` удалены — D-17)
- **Достижения при поражении** (зад. 47, 48): новое поле `defeat_achievements` в `TaskInfoEntity` — при принятии наград поражения достижения сохраняются.
- **Условие QUEST_NOT_AVAILABLE**: новый тип `TaskConditionKind` — открытие задания, если другое задание недоступно (зад. 42).
- **Каталог глав**: синхронизирован с `doc/compainInfo.md` — условные задания 42-49 и истечения 47, 48 (гл. 5, 6, 8, 9).
- **Отступ 1 мм в BattleScreen**: корневой `Column` имеет `Modifier.padding((160f/25.4f).dp)` — физический 1 мм от краёв.

---

## 8. Технический стек

| Компонент | Технология |
|-----------|-----------|
| Язык | Kotlin 2.0.21 |
| Платформа | Kotlin Multiplatform (KMP) |
| UI (Android) | Jetpack Compose + Material3 |
| UI (iOS) | SwiftUI |
| Архитектура | MVVM + Clean Architecture (Repository) |
| Хранение данных | Room KMP 2.6.1 (SQLite) в shared/commonMain |
| Кодогенерация БД | KSP 2.0.21-1.0.28 |
| Управление состоянием | `StateFlow<BattleScreenState>`, `StateFlow<CampaignUiState>` |
| Асинхронность | Kotlin Coroutines (`delay`, `Job`, `SupervisorJob`) |
| Кросс-платформенное время | expect/actual `currentTimeMillis()` |
| Тестирование | `kotlin.test` (commonTest) |
| Сборка | Gradle 8.7 + Kotlin DSL + Version Catalog |
| Min SDK | API 26 (Android 8.0) |
| Gradle wrapper | `gradlew` / `gradlew.bat` |
