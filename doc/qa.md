# Вопросы и ответы по проекту Primal App

## A. Вопросы, решённые разработчиком самостоятельно (best practices)

### 1. На какой платформе разрабатывать?

**Решение:** Kotlin Multiplatform (KMP) — общая бизнес-логика в `shared/`, платформенный UI в `androidApp/` (Compose) и `iosApp/` (SwiftUI).

**Реализация:** `shared/build.gradle.kts:1` — `kotlin("multiplatform")` с targets `androidTarget`, `iosX64`, `iosArm64`, `iosSimulatorArm64`.

---

### 2. Какой UI-фреймворк?

**Решение:** Jetpack Compose + Material3 для Android. Shared-модуль не содержит UI-кода — только модели, ViewModel и бизнес-логику.

**Реализация:** `androidApp/build.gradle.kts:1` — плагин `composeCompiler`, зависимости `compose-bom`, `compose-ui`, `compose-material3`.

---

### 3. Какая архитектура?

**Решение:** MVVM. `BattleViewModel` (`BattleViewModel.kt:49`) управляет состоянием через `MutableStateFlow<BattleScreenState>`. UI наблюдает через `collectAsState()` (`MainActivity.kt:55`).

---

### 4. Как хранить данные на устройстве?

**Решение:** Room KMP 2.6.1. Вся БД (7 Entity, 7 DAO, mapper, CampaignRepositoryImpl) расположена в `shared/src/commonMain/kotlin/com/primalapp/database/`. Platform-specific код (Room.databaseBuilder, currentTimeMillis) вынесен в expect/actual.

**Реализация:**
- `shared/.../database/Platform.kt` — expect-декларации
- `shared/.../database/Platform.android.kt` — Android actual (`Context`, `Room.databaseBuilder`)
- `shared/.../database/Platform.ios.kt` — iOS actual (`NSFileManager`, `Room.databaseBuilder`)
- `shared/.../database/PrimalDatabase.kt` — `@Database` с 7 таблицами
- `shared/.../database/CampaignRepositoryImpl.kt` — полная реализация CampaignRepository

---

### 5. Как реализовать таймер 2 секунды для добора урона?

**Решение:** `Job` корутины с `delay(2000)`. Каждое нажатие кнопки отменяет предыдущий Job и запускает новый. Значение суммируется в `pendingDamage`.

**Реализация:** `BattleViewModel.kt:84–98` — метод `addDamage`:
```kotlin
timerJob?.cancel()                          // отмена предыдущего
pendingDamage += amount                     // накопление
timerJob = scope.launch { delay(2000); commitDamage() }  // автоприменение
```

---

### 6. Как обрабатывать ввод урона с клавиатуры?

**Решение:** `OutlinedTextField` с `KeyboardType.Number` + кнопки быстрого добавления. Валидация: `toIntOrNull() ?: return`.

**Реализация:** `MainActivity.kt:226–245` — поле + кнопка OK, вызов `viewModel.setManualDamage(dmg)` (`BattleViewModel.kt:101–109`).

---

### 7. Какой минимальный SDK?

**Решение:** API 26 (Android 8.0, 2017).

**Реализация:** `androidApp/build.gradle.kts:12` — `minSdk = 26`.

---

### 8. Как организовать фазы боя?

**Решение:** `enum class FightPhase` (`BattleViewModel.kt:23–31`) — 7 состояний: `PRE_BATTLE`, `SETUP`, `PHASE_I`, `PHASE_II`, `PHASE_III`, `VICTORY`, `DEFEAT`. Переходы управляются через `when` в `commitDamage()` и `endRound()`.

---

### 9. Как хранить до 10 кампаний и переключаться?

**Решение:** Room-таблица `campaigns` (max 10 проверяется через `getMaxCampaigns() = 10`). Каскадные связи: Campaign → Hunters → Skills, Resources; Campaign → Achievements, Trophies, Quests. UI — `MainMenuScreen` → `CampaignListScreen` → `CampaignSheetScreen`.

**Реализовано:** `shared/.../database/` (Room KMP), `shared/.../viewmodel/CampaignViewModel.kt`, `androidApp/.../ui/CampaignListScreen.kt`.

---

### 10. Структура классов данных?

**Решение:** `data class` c мутабельными `var` полями для часто меняющегося состояния (здоровье, ярость). Иммутабельные `val` для констант. Computed-свойства (`isAlive`, `healthPercentage`, `isLastPhase`).

**Реализация:**
- `Monster.kt:3–21` — 2 `val` + 9 `var` + 1 computed
- `Hunter.kt:3–16` — 2 `val` + 2 `var` + 2 computed

---

## B. Вопросы, заданные пользователю

### 11. Что означает статус «затвердевший»?

**Вопрос:** Как работает механика «затвердевший»?

**Ответ:** Остаток урона после нанесения раны сгорает (не переносится). Без статуса — остаток сохраняется.

**Реализация:** `MonsterExt.kt:45–51`:
```kotlin
val remaining = if (isHardened && wounds > 0) {
    val leftover = accumulatedDamage
    accumulatedDamage = 0       // остаток сгорает
    leftover
} else {
    accumulatedDamage           // остаток сохраняется
}
```

**Тест:** `MonsterTest.kt:83–111` — 2 теста (hardened vs non-hardened при 8 и 9 урона).

---

### 12. Как работает таймер на 2 секунды?

**Вопрос:** Сбрасывается ли таймер при новом нажатии?

**Ответ:** Сброс при каждом нажатии.

**Реализация:** `BattleViewModel.kt:91` — `timerJob?.cancel()` перед запуском нового. Каждое нажатие (+1/+10/+50) перезапускает 2-секундный отсчёт.

---

### 13. Какой тип проекта: Android или KMP?

**Вопрос:** Чистый Android или Kotlin Multiplatform?

**Ответ:** KMP (Android + iOS).

**Реализация:** `settings.gradle.kts:12–13` — `include(":shared")` и `include(":androidApp")`. Структура: `shared/src/commonMain`, `androidApp/`, `iosApp/`.

---

## C. Вопросы, выявленные при реализации

### 14. Как обрабатывать смену стойки в середине цепочки ран?

**Проблема:** При большом уроне (например, 8 урона при `damageForWound=4`) наносится 2 раны. Если после первой раны здоровье падает ниже `healthForStanceChange`, нужно сменить фазу — но вторая рана наносится уже с новыми параметрами.

**Решение:** Фаза проверяется и меняется **между ранами** внутри цикла `while`. Сразу после `currentPhase++` следующие итерации цикла продолжают использовать текущий `damageForWound` (он не изменился до вызова `resetPhase` из ViewModel).

**Реализация:** `MonsterExt.kt:22–42` — цикл `while (accumulatedDamage >= damageForWound)` с проверкой `currentPhase < maxPhases` внутри.

**Тест:** `MonsterTest.kt:53–66` — проверка фазового перехода при 8 урона, `healthForStanceChange=8`.

---

### 15. Почему `toggleHardened()` проверяет `wounds > 0`?

**Проблема:** Если `isHardened = true`, но ни одной раны не нанесено (урон меньше `damageForWound`), остаток НЕ должен сгорать — он копится к следующей ране.

**Решение:** Условие `isHardened && wounds > 0`. Сгорание только если была хотя бы одна рана.

**Реализация:** `MonsterExt.kt:45` — `if (isHardened && wounds > 0)`.

---

### 16. Как предотвратить игнорирование ручного ввода урона?

**Проблема:** Если пользователь вводит урон руками, а через 2 секунды таймер от предыдущего нажатия кнопки доберёт остаток — получится двойное применение.

**Решение:** `setManualDamage` (`BattleViewModel.kt:101–109`) отменяет активный таймер (`timerJob?.cancel()`) и НЕ запускает новый. Урон применяется только по кнопке «Применить урон сейчас» или через ручной вызов `commitDamage()`.

---

### 17. Как обеспечивается потокобезопасность StateFlow?

**Решение:** `MutableStateFlow` гарантирует атомарные обновления через `update {}`. Все изменения состояния проходят через `_state.update { it.copy(...) }`.

**Реализация:** Например, `BattleViewModel.kt:134–144` — `_state.update { it.copy(phase = newPhase, monster = current.monster, ...) }`.

---

## D. Тестовое покрытие

| Файл | Количество тестов | Что проверяется |
|------|------------------|-----------------|
| `MonsterTest.kt` | 12 | Раны, урон, hardened, фазы, добивание, сброс фазы, ярость |
| `HunterTest.kt` | 10 | Урон, лечение, потеря сознания, воскрешение, крит, проценты |
| **Всего** | **22** | Полное покрытие моделей и функций-расширений |

---

## E. Вопросы по доработке «Лист компании» (v2)

### 18. Классы персонажей — 4 или 6?

**Вопрос (27.07.2026):** В требовании перечислено 6 классов: Кара, Хелерен, Дареон, Мира, Торег, Льонар. В базовой книге правил только 4. Откуда Кара и Хелерен?

**Ответ:** Кара и Хелерен из дополнений. Использовать все 6 классов.

**Реализация:** `HunterClass.kt` — enum из 6 значений: DAREON, MIRA, TOREG, LIONAR, KARA, HELEREN.

---

### 19. Количество материй — 6 или 7?

**Вопрос (27.07.2026):** В требовании указано 6 видов материй, в правилах — 7: Кости, Кристалл, Кровь, Чешуя, Зимия, Иридия, Златия.

**Ответ:** Использовать 7 материй как в правилах.

**Реализация:** `Material.kt` — enum из 7 значений.

---

### 20. Главы апгрейда кузни/лаборатории

**Вопрос (27.07.2026):** Подтверждены ли главы 3 и 7 для апгрейда? В правилах точные номера не зафиксированы.

**Ответ:** Главы 3 и 7 (как в требовании).

**Реализация:** `domain/ChapterProgression.kt:10-11` — методы `getForgeUpgradeChapter(2) → 3`, `getForgeUpgradeChapter(3) → 7`.

---

### 21. Исключение первого боя

**Вопрос (27.07.2026):** Что означает «кроме первого боя» в требовании?

**Ответ:** Помечать все бои, включая первый (Пролог — полноценная глава).

---

### 22. Название режима: «Быстрый бой» или «Режим экспедиции»?

**Вопрос (27.07.2026):** Какой термин использовать в интерфейсе?

**Ответ:** «Режим экспедиции» (как в официальных правилах).

---

### 23. Список 9 стихий

**Вопрос (27.07.2026):** Какие именно 9 стихий использовать?

**Ответ:** Полный список из правил — 6 базовых (Огонь, Рог, Коралл, Кристалл, Молния, Металл) + 3 дополнения (Перо, Яд, Лёд).

**Реализация:** `Element.kt` — enum из 9 значений с флагом `isExpansion`.

---

## F. Результаты реализации слоя данных (27.07.2026)

**Перенесён в Task4 (28.07.2026) — Room перемещён из androidApp в shared/commonMain.**

### Созданные/изменённые файлы

**shared/commonMain — доменный слой (13 файлов):**
| Файл | Назначение |
|------|-----------|
| `model/campaign/Campaign.kt` | Кампания: id, имя, глава, кузня, лаборатория, заметки |
| `model/campaign/CampaignHunter.kt` | Охотник в кампании: имя игрока + класс |
| `model/campaign/SkillNode.kt` | Узел древа навыков: ветвь + ступень + флаг |
| `model/campaign/Achievement.kt` | Достижение |
| `model/campaign/Trophy.kt` | Трофей (босс, стихия, глава) |
| `model/campaign/Quest.kt` | Задание: id, глава, стихия, статус |
| `model/campaign/ResourceEntry.kt` + `ResourceType.kt` | Запись о ресурсе + enum типа |
| `model/campaign/HunterClass.kt` | 6 классов (Дареон, Мира, Торег, Льонар, Кара, Хелерен) |
| `model/campaign/SkillBranch.kt` | 5 ветвей (А, Б, В, Г, Д) |
| `model/campaign/Material.kt` | 7 материй |
| `model/campaign/Plant.kt` | 6 растений |
| `model/campaign/Element.kt` | 9 стихий (6 базовых + 3 дополнения) |
| `repository/CampaignRepository.kt` | Интерфейс: CRUD кампаний, охотников, навыков, ресурсов, обмен, главы |
| `domain/SkillValidator.kt` + `SkillValidatorImpl.kt` | Валидация разблокировки навыков |
| `domain/ResourceExchangeValidator.kt` + `ResourceExchangeValidatorImpl.kt` | Валидация обмена (1:1, same-type) |
| `domain/ChapterProgression.kt` + `ChapterProgressionImpl.kt` | Продвижение глав, апгрейд кузни/лаборатории |

**androidApp — слой Room (удалён 28.07.2026, перенесён в shared):**

| Файл | Назначение | Статус |
|------|-----------|--------|
| `database/*` | Все файлы БД | Перенесены в `shared/.../database/` |

**Изменённые конфиги:**
- `gradle/libs.versions.toml` — KSP 2.0.21-1.0.28, Room 2.6.1, kotlinx-datetime 0.6.1
- `build.gradle.kts` — плагин KSP
- `shared/build.gradle.kts` — KSP + Room KMP + kotlinx-datetime (добавлено 28.07.2026)
- `androidApp/build.gradle.kts` — KSP и Room удалены (28.07.2026, транзитивно через shared)

### Coverage требований

| Требование | Статус | Где реализовано |
|-----------|--------|-----------------|
| Дерево навыков А1-Д2, tier 2 требует tier 1 | ✅ | `SkillValidatorImpl`, `initSkillTree()` |
| 7 материй, ввод числа, обмен | ✅ | `ResourceEntity`, `exchangeResources()` |
| 6 растений, аналогично материям | ✅ | тот же механизм |
| 9 стихий, аналогично | ✅ | тот же механизм |
| Заметки | ✅ | `Campaign.notes` |
| Глава авто-инкремент + ручной режим | ✅ | `advanceChapter()`, `updateChapter()` |
| Кузня/лаб: старт 1, гл.3→2, гл.7→3 | ✅ | `ChapterProgressionImpl` |
| Пост-победный флоу | ✅ | `saveVictory()` |
| Выбор «Режим экспедиции» / «Режим кампании» | ✅ | `MainMenuScreen` + `onQuickBattleSelected()` / `onCampaignModeSelected()` |
| Название кампании + выбор классов | ✅ | `createCampaign()`, `addHunters()` |
| Автосохранение до 10 кампаний | ✅ | `getCampaignCount()`, `getMaxCampaigns()=10` |
| UI / ViewModel | ✅ | `CampaignViewModel`, все экраны в `androidApp/.../ui/` |

### Результат сборки

```
BUILD SUCCESSFUL in 6s
30 actionable tasks: 4 executed, 26 up-to-date
```

## G. Task4: Миграция Room в shared (28.07.2026)

### 24. Почему Room перемещён из androidApp в shared?

**Причина:** Обеспечить единый слой данных в KMP-модуле для Android и iOS. До миграции Room находился в `androidApp/`, что делало его недоступным для iOS-таргета.

**Реализация:** Весь слой БД (7 Entity, 7 DAO, mapper, PrimalDatabase, CampaignRepositoryImpl, Platform expect/actual) перемещён из `androidApp/.../database/` в `shared/.../database/`.

### 25. Какие KMP-адаптации потребовались?

| Проблема | Решение |
|----------|---------|
| `System.currentTimeMillis()` недоступен в commonMain | `expect fun currentTimeMillis()`: Android → `System.currentTimeMillis()`, iOS → `NSDate().timeIntervalSince1970 * 1000` |
| `Room.databaseBuilder()` требует платформенный контекст | `expect fun createPrimalDatabase(PlatformContext)`: Android → `Context`, iOS → `String` (путь к БД) |
| Entity default values с `System.currentTimeMillis()` | Заменены на `currentTimeMillis()` |
| CampaignRepositoryImpl с `System.currentTimeMillis()` | Заменён на `currentTimeMillis()` |
| Converters.kt (пустой) | Удалён |

### 26. Почему Room KMP 2.6.1, а не SQLDelight?

- Room сохраняет 100% существующей логики (аннотации `@Entity`/`@Dao`/`@Database` работают в commonMain без изменений)
- SQLDelight потребовал бы переписывания всех DAO в `.sq` файлы
- Room 2.6.x имеет KMP-поддержку (commonMain + platform-specific через expect/actual)

### 27. Что изменилось в androidApp?

- `MainActivity.kt`: импорт `CampaignRepositoryImpl`, `PlatformContext`, `createPrimalDatabase` теперь из `com.primalapp.database` вместо `com.primalapp.android.database`
- Создание БД: `createPrimalDatabase(context as PlatformContext)` вместо `Room.databaseBuilder(context, PrimalDatabase::class.java, "primal.db").build()`
- `build.gradle.kts`: удалены KSP и Room-зависимости (транзитивно через shared)
- Удалён весь каталог `androidApp/.../database/`

---

## H. Задача 1: Лейбл + авто-расчёт здоровья (02.08.2026)

### 28. Расхождение с правилами по начальному здоровью

**Проблема:** В правилах игры (с. 10) начальное здоровье монстра — 10. Формула `hunterCount * damageForWound` при значениях по умолчанию (2 × 4) даёт 8.

**Ответ:** Приложение — компаньон, формула удобнее для игроков. Использовать `hunterCount * damageForWound`.

### 29. Где менять лейбл «Урон для нанесения раны на игрока»?

**Ответ:** Только в `PhaseChangeDialog` (BattleScreen.kt:206). В `PreBattleScreen` (строка 61) лейбл уже корректный.

### 30. Где менять расчёт здоровья босса?

**Ответ:** В `BattleViewModel.startBattleWithHunters()` — единственное место создания Monster с hardcoded `currentHealth = 10`. Метод `startBattle()` делегирует в `startBattleWithHunters()`.

---

## I. Задача 2: endRound + hardened (04.08.2026)

### 31. Почему hardened проявляет баг смены стойки заметнее?

**Ответ:** При hardened `accumulatedDamage` сгорает после ран, `currentPhase` в модели меняется, а UI не обновляется, потому что `endRound()` игнорировал `DamageResult`. Без hardened баг тоже есть, но менее заметен — accumulatedDamage сохраняется и фаза менялась бы при следующем `commitDamage()`.

### 32. Почему используется маппинг `result.newPhase` вместо чтения `currentPhase` из модели?

**Ответ:** `currentPhase` может измениться несколько раз за один вызов `takeDamage()` (при большом уроне), но `result.newPhase` фиксирует последнюю фазу. Маппинг из `commitDamage()` уже зарекомендовал себя — используем тот же подход.

---

## J. Задача 3: документация 9 стоек (04.08.2026)

### 33. Почему в коде уже 9 стоек, а в документации 3?

**Ответ:** Код был расширен до 9 стоек (`FightPhase.PHASE_I…PHASE_IX`, `maxPhases = 9`), но документация не обновлялась. Задача 3 — синхронизировать документацию с реализацией.

---

## K. Задачи 12.4–14.x, 18.4, 20.1 (18.08.2026)

### 34. Статус задач без маркера «ВЫПОЛНЕНО»

**Проблема:** В `implementationTasks.md` задачи 12.4, 12.5, 13.1–13.4, 14.1–14.4 не помечены «✅ ВЫПОЛНЕНО» (секции 13.x/14.x дополнительно повреждены в кодировке), поэтому непонятно, что из них осталось реализовать.

**Ответ (пользователь):** По итогам ревизии кода задачи 12.4, 12.5, 13.x и 14.x фактически реализованы и покрыты тестами. Считать их выполненными. Итерация выполняется в порядке **18.4 → 20.1** — это единственные фактически не реализованные задачи.

**Реализация (подтверждение по коду):**
- `CampaignViewModel.getDifficultyForChapter()` — 12.4
- `BattleViewModel` (автозаполнение стоек в `commitDamage`/`endRound`/`onManualStanceChange` через `selectedBoss.getStance(...)`) — 12.5
- `CampaignViewModel.onPreBattleBossSelected/resolvePreBattleBoss/onPreBattleDifficultySelected`, `preBattleHunterCount` — 13.x/14.x

### 35. Правки тестов в задаче 20.1

**Проблема:** Задача 20.1.2 предписывает обновить существующий тест `MonsterTest.resetPhase resets damage and updates thresholds` (сейчас ожидает `accumulatedDamage == 0`, после фикса должно быть `5` для не-затвердевшего) и добавить тест на затвердевшего. Но действует жёсткое правило «не вносить правки в тесты».

**Ответ (пользователь):** Оформить нужные правки тестов отдельной задачей в `testTasks.md`, сами тестовые файлы не трогать.

**Следствие:** После исправления `resetPhase()` тест `resetPhase resets damage and updates thresholds` временно станет красным — задача на его обновление зафиксирована в `testTasks.md`.

---

## L. Задача 22.x: Коровон — стойка без порога раны (18.08.2026)

### 36. Механика «стойка без порога раны»

**Проблема:** У Коровона на 2-й стойке не указана прочность (`damageForWound = —`), а в правилах сказано «значение прочности всегда обозначено числом». В текстовой выгрузке правил это исключение не описано (значение на карте-картинке).

**Ответ (пользователь):**
- Источник истины — запрос. `damageForWound` становится nullable (`Int?`) — общая возможность, не спец-случай для Коровона.
- На стойке без прочности урон только накапливается (`accumulatedDamage`), раны не наносятся, победа на такой стойке невозможна.
- При подтверждении смены стойки со стойки без порога (dfw=null) на стойку с порогом — весь `accumulatedDamage` **немедленно** пересчитывается в раны по прочности новой стойки (не ждёт следующего ввода урона). Поведение обычных боссов (перенос остатка из 20.1) не меняется.

**Реализация:** nullable `BossStance.damageForWound`, `Monster.damageForWound`, колонки `stance1_dfw…stance3_dfw` (`BossEntity`), `MIGRATION_6_7` (v6→7) + seed Коровона, null-безопасность в `MonsterExt.takeDamage/healWound/resetPhase`, `BattleViewModel.confirmPhaseChange` (nullable dfw + немедленное нанесение через `takeDamage(0)`), UI `PhaseChangeDialog` («нет порога раны»).

---

## M. Задача 23.x: 9 боссов + ALL_BOSS_NAMES (18.08.2026)

### 37. Выпадающий список победы (ALL_BOSS_NAMES)

**Проблема:** `CampaignViewModel.ALL_BOSS_NAMES` содержал 13 базовых боссов и используется в `onVictory()` как выпадающий список `defeatedBosses` в PostVictoryDialog. Из 9 новых боссов Оруксен/Фелаксир/Таррагуа уже были в списке, а Тараск/Кситерос/Зекат/Зекалит/Пазис/Нагарджас — нет.

**Ответ (пользователь):** Дополнить `ALL_BOSS_NAMES` до **19** монстров — все новые боссы должны появляться в выпадающем списке победы.

**Следствие:** Существующий тест `ALL_BOSS_NAMES содержит 13 боссов` временно красный — задача на его обновление (ожидать 19) зафиксирована в `testTasks.md` (задача 23.3).

---

## N. Задача 24.x: SQL-ошибка миграции 5→6 (18.08.2026)

### 38. NOT NULL constraint при обновлении БД

**Проблема:** При выборе режима кампании приложение падает с SQL-ошибкой `NOT NULL constraint failed: bosses.stance2_dfw`.

**Причина:** Общий `seedBosses()` (используется миграциями и `onCreate`-колбэком) после задачи 22.1 вставляет Коровона со `stance2_dfw = NULL`, но `MIGRATION_5_6` создавал таблицу `bosses` со `stance1_dfw…stance3_dfw INTEGER NOT NULL`. При обновлении БД с версии ≤5 миграция 5→6 падала. `MIGRATION_6_7`/`MIGRATION_7_8` уже использовали nullable dfw — рассинхрон схем.

**Ответ (пользователь):** Причина подтверждена (`NOT NULL constraint failed: bosses.stance2_dfw`). Объём — фикс + рефакторинг (единый источник схемы).

**Реализация:** Вынесен `CREATE_BOSSES_TABLE` (nullable dfw) и хелпер `recreateAndSeedBosses(db)` = DROP + CREATE + `seedBosses`. Тела `MIGRATION_5_6`/`MIGRATION_6_7`/`MIGRATION_7_8` заменены на вызов хелпера. Исторические миграции `2_3`/`3_4`/`4_5` не тронуты.

---

## O. Задачи 25.x–26.x: сортировка + опциональный dfw + кнопка +5 (18.08.2026)

### 39. Сортировка боссов и опциональный dfw

**Ответ (пользователь):**
- Сортировка «по стихии» — по алфавиту русских названий стихий (`displayName`): Коралл, Кристалл, Металл, Молния, Огонь, Перо, Рог.
- «Ввести данные вручную» — первым; «Пробуждённый» (null-стихия) — последним.
- Пустое поле «урон для нанесения раны» = нет порога раны (урон накапливается, как у Коровона на 2-й стойке) — применимо и в меню смены стойки, и в pre-battle.

**Реализация:**
- `CampaignViewModel.loadBosses()` — сортировка `compareBy { element == null }.thenBy { element?.displayName }.thenBy { name }`.
- `MainActivity` — «Ввести данные вручную» первым в обоих pre-battle.
- `PhaseChangeDialog`/pre-battle — поле dfw опционально (пусто → null), подпись «(пусто = нет порога раны)».
- `startBattle`/`startBattleWithHunters`/`onConfirmCampaignBattleStart` — `damageForWound: Int?`.
- `BattleScreen` — кнопка `+5` (`onQuickButtonPress(5)`).

---

## P. Задача 28.x: доработка режима кампании (18.08.2026)

### 40. Ресурсы, задания, лист кампании

**Ответ (пользователь):**
- Материи: «Златия» (не «Здатия»), убрать «Кристалл» — 6 материй: Чешуя, Кости, Кровь, Зимия, Иридия, Златия.
- Растения (порядок): Ниллея, Тармарет, Альбалацея, Меллис, Антемон, Селикорния.
- После победы вводить материи и растения (помимо стихии босса), начислять **всем охотникам**.
- После поражения — выбор номера открывшегося задания (становится доступным).
- Перед боем — выбор активного задания из открытых (завершается при победе).
- Кнопки +/− у ресурсов — по 1, минимум 0.

**Реализация:**
- `Material` — 6 значений (без `CRYSTAL`), переупорядочены; `Plant` — переупорядочены.
- `onConfirmVictory` — задания открываются как доступные (убран сломанный `nextQuestId` через запятую), обёрнуто в `try-catch` (фикс краша).
- Ввод материй/растений в `PostVictoryDialog` (`victoryMaterials`/`victoryPlants`) + начисление всем охотникам.
- Поражение: `onBattleFinished` сохраняет выбранные номера как доступные задания (`selectedDefeatQuestNumbers`).
- Перед боем: `onStartCampaignBattle` → диалог выбора активного задания (`activeQuestId`), завершается при победе.
- Лист кампании: задания (с переключением выполнено/открыто), поверженные боссы и их стихии; +/− кнопки у ресурсов.

**Примечание:** `uncompleteQuest` добавлен в интерфейс `CampaignRepository` с default no-op (чтобы не ломать фейковый репозиторий в существующих тестах); реальная реализация в `CampaignRepositoryImpl`.

---

## Q. Задача 27.1: инфраструктура тестов миграций Room (20.08.2026)

### 41. Как выполнять задачу 27.1 при запрете «не вносить правки в тесты»?

**Проблема:** Задача 27.1 по своей сути требует создать тест-класс миграций (`shared/src/androidUnitTest`), что противоречит инструкции «ни при каких не вноси правки в тесты».

**Ответ (пользователь):** Выполнить **только инфраструктуру** (добавить зависимости `room-testing`/Robolectric и source set `androidUnitTest`), а написание самого тест-класса оформить отдельной задачей в `testTasks.md`.

**Следствие:** `testTasks.md` — задача на создание тест-класса миграций (покрытие 24.2); `implementationTasks.md` 27.1 помечена как реализованная (инфраструктура без тестов).

---

### 42. Куда фиксировать Q&A?

**Проблема:** В инструкции указан путь `./primalApp/qa.md`, но в проекте файл находится в `doc/qa.md`.

**Ответ (пользователь):** Использовать существующий `doc/qa.md`.

---

## R. Задача 29.1: фикс сохранения после победы (20.08.2026)

### 43. Охват фикса сохранения после победы

**Проблема:** Дефект: после победы над первым боссом и нажатия «Продолжить» в PostVictoryDialog визуально ничего не происходит; после повторных нажатий открытые задания дублируются.

**Ответ (пользователь):**
- Исправить все три связанных дефекта одним блоком: (1) дедупликация заданий (идемпотентный `saveQuest`), (2) начисление ресурсов реальным охотникам (не «фантому» с `id=0`), (3) надёжный переход после сохранения (гарантированное закрытие диалога и переход в CampaignSheet).
- Защита от повторного нажатия: **блокировать кнопку «Продолжить» на время сохранения** (`isSaving`).

**Реализация:**
- `QuestEntity` — уникальный индекс `(campaign_id, quest_id)`; миграция `MIGRATION_8_9` с чисткой дубликатов; `QuestDao` — апсерт вместо слепого `INSERT`.
- `onStartCampaign()` — после `addHunters` перечитывать реальных охотников через `getHunters(campaignId)` в `state.hunters`.
- `onConfirmVictory()` — `isSaving = true/false` в `finally`; `PostVictoryDialog` — `enabled = !state.isSaving` у кнопки «Продолжить»; гарантированный `showPostVictory = false` + `loadCampaignSheet()` после успешного сохранения.

---

## S. Задача 24.2/27.2: тесты миграций Room (20.08.2026)

### 44. Куда писать тесты миграций Room?

**Проблема:** Задача 24.2 требует инструментальных тестов миграций (`MigrationTestHelper` + Robolectric), которые физически не выполняются в commonTest. Инструкция предписывает писать тесты в `commonTest`, но для миграций это невозможно.

**Ответ (пользователь):** Писать тесты миграций в `shared/src/androidUnitTest/...` (Robolectric + `MigrationTestHelper`). Инфраструктура (room-testing, robolectric, androidx.test:core, source set `androidUnitTest`) уже настроена в задаче `implementationTasks.md` 27.1.

**Следствие:** `testTasks.md` 24.2 и 27.2 покрываются одним файлом `MigrationTest.kt` в `shared/src/androidUnitTest`.

---

### 45. До какой версии мигрировать в тестах?

**Проблема:** В задаче 24.2 описана миграция «5→6→7→8», но БД теперь версии **9** (добавлен `MIGRATION_8_9` — уникальный индекс `quests(campaign_id, quest_id)`).

**Ответ (пользователь):** Мигрировать до **v9** (4→9, 5→9), проверяя 19 боссов и уникальный индекс заданий.

---

### 46. Schema export для валидации миграций?

**Проблема:** `MigrationTestHelper.runMigrationsAndValidate()` требует экспортированную схему Room (`exportSchema = true` + каталог схем в KSP), а сейчас `exportSchema = false`.

**Ответ (пользователь):** Сразу включить `exportSchema = true` и настроить KSP `room.schemaLocation`. Правка реализации одобрена пользователем в рамках тестовой задачи.

---

### 47. Обнаруженный баг: onCreate-seed падает на чистой установке

**Проблема:** Тест `MigrationTest.onCreate seed на чистой установке` (Robolectric) упал с `SQLiteConstraintException: NOT NULL constraint failed: bosses.stance4_dfw`.

**Причина:** На чистой установке таблица `bosses` создаётся Room из `BossEntity` (exportSchema): `stance4_dfw INTEGER NOT NULL` и `stance5_dfw INTEGER NOT NULL` **без** `DEFAULT 0` (Kotlin-default `= 0` не транслируется в SQL DEFAULT). Функция `seedBosses.insert3*` вставляет только `stance1..3` → NOT NULL violation. Миграции работают лишь потому, что `CREATE_BOSSES_TABLE` объявляет `DEFAULT 0`.

**Решение:** Зарегистрирована задача `implementationTasks.md` 30.1 (дополнить seed стойками 4–5 и/или добавить `@ColumnInfo(defaultValue = "0")` в `BossEntity`). Правки реализации в рамках QA-сессии не вносились.

---

## T. Задача 30.1: фикс onCreate-seed боссов (20.08.2026)

### 48. Какой способ фикса применить?

**Проблема:** Задача 30.1 предлагает два варианта: (1) править `seedBosses.insert3*` (дополнить `stance4_dfw = 0`, `stance5_dfw = 0`), (2) добавить `@ColumnInfo(defaultValue = "0")` в `BossEntity.stance4Dfw`/`stance5Dfw`. Вариант 2 меняет экспортированную схему Room (9.json), что потребует обновления тестовых фикстур схем — а это правка тестов (запрещено).

**Ответ (пользователь):** Применить **оба подхода**: добавить `defaultValue = "0"` в `BossEntity` (чтобы Room-схема совпала с `CREATE_BOSSES_TABLE`) **и** дополнить `seedBosses.insert3*` стойками 4–5 (надёжность независимо от схемы).

### 49. Оставить ли расхождение схемы?

**Проблема:** Room-схема (из `BossEntity`, без DEFAULT) и `CREATE_BOSSES_TABLE` (с `DEFAULT 0`) расходятся.

**Ответ (пользователь):** Применить вариант 1 (правка seed) **+** оформить отдельную задачу на синхронизацию схемы (обновление экспортированного 9.json и тестовых фикстур схем) в `implementationTasks.md`.

---

## U. Задача 31.2: условные правила наград + каталог глав (20.08.2026)

### 50. Задания 41, 46 в compainInfo.md

**Проблема:** compainInfo.md (гл.2, гл.4) ссылается на задания 41 и 46, но каталог taskInfo.md заканчивается на 40.

**Ответ (пользователь):** Оставить ссылки 41/46 как есть (в seed глав); при открытии задание создаётся с номером, но без данных каталога (taskInfo его не содержит).

### 51. Начисление наград глав

**Проблема:** Глава 1 имеет награды (материи/растения).

**Ответ (пользователь):** Начислять каждому охотнику (как и награды заданий).

### 52. Каталог достижений

**Проблема:** Условия глав ссылаются на достижения («если есть достижение X»).

**Ответ (пользователь):** Без отдельного каталога — сравнение по строковому имени достижения (как сейчас, свободный ввод).

### 53. Улучшение набора охотника

**Проблема:** «Улучшение набора охотника» в главах 3,5,7,8,9,10.

**Ответ (пользователь):** Хранить как флаг + текст в `ChapterInfo` (`hunterKitUpgrade: Boolean`), показывать в окне наград главы текстом.

### 54. Структура условных правил

**Проблема:** Как хранить условные правила открытия заданий («если глава…», «если есть достижение…», «иначе…»).

**Ответ (пользователь):** Структурированный тип `TaskCondition(kind, achievementName?, chapterSet?, questNumber?, elseQuestNumber?)`, сериализуется текстом в `TaskInfoEntity`; вычисление в домене.

### 55. Интерактивное решение гл.7

**Проблема:** «Тренироваться у подножья Волтьяра» Да/Нет → достижение «Голос Волтьяра».

**Ответ (пользователь):** Структурированный тип `ChapterDecision(question, options=[Да, Нет], achievementOnYes=«Голос Волтьяра»)`, сериализуется в `ChapterInfoEntity`; диалог показывается в окне наград главы (31.6).

### 56. Истекшие задания

**Проблема:** «Истекло время заданий N» в главах.

**Ответ (пользователь):** В `ChapterInfo` хранить `expireQuests: List<Int>`; при «Принять» в окне главы задания помечаются недоступными (`isAvailable = false`).

---

## V. Задача 31.3: сортировка и отображение заданий (20.08.2026)

### 57. Состав списка заданий в листе кампании

**Проблема:** Текущий лист кампании показывает все задания с кнопкой «Выполнено/Открыть». Задача 31.3 требует сортировку открытых заданий по возрастанию номера.

**Ответ (пользователь):** Показывать **только открытые** задания (`isAvailable = true`), отсортированные по `questNumber` по возрастанию. Выполненные задания в листе не отображать.

### 58. Отображение названия и босса в диалоге выбора задания

**Проблема:** Отображение «название + босс (стихия)» — только в листе кампании или также в `QuestSelectDialog`.

**Ответ (пользователь):** Обновить **оба** места — и лист кампании, и `QuestSelectDialog` (выбор активного задания перед боем).

---

## W. Задача 31.4: 2 стихии босса + предзаполнение Вираксена (20.08.2026)

### 59. Начисление 2 стихий и предзаполнение пролога

**Проблема:** Задача 31.4: (1) награда за победу — 2 стихии поверженного босса каждому охотнику вместо 1; (2) в прологе предзаполнять Вираксена сложности 0.

**Ответ (пользователь):** Реализовано без дополнительных уточнений (детали уже зафиксированы в задаче): `addResourceToAll(..., 2)` в `onConfirmVictory`; в `startBattleInternal` для `isPrologue = true` — `selectedPreBattleBossName = "Вираксен"`, `preBattleDifficulty = 0`, после `loadBosses()` вызывается `resolvePreBattleBoss()`.

**Следствие:** Тест 28.2 (`onConfirmVictory начисляет материи и растения каждому охотнику`) ожидает 1 единицу FIRE — теперь 2; задача на правку теста зарегистрирована в `testTasks.md` 31.4T.

---

## X. Задача 31.5: окно наград за задание (20.08.2026)

### 60. Окно наград при поражении

**Проблема:** Сейчас после поражения показывается `DefeatScreen` с ручным выбором номеров заданий (1..49) и кнопкой «К листу кампании». Задача 31.5 требует окно наград за поражение с «Принять/Редактировать».

**Ответ (пользователь):** Заменить `DefeatScreen` на то же окно `QuestRewardsDialog` с наградами за поражение из `TaskInfo` (`defeatOpenQuests`) и кнопками «Принять»/«Редактировать».

### 61. По какому заданию брать награды при победе

**Проблема:** Награды за задание берутся из каталога `TaskInfo`.

**Ответ (пользователь):** По **активному заданию** (`activeQuestId`/`questNumber`); если активного задания нет — окно с пустыми наградами (только 2 стихии босса + ввод вручную через «Редактировать»).

### 62. Кнопка «Редактировать»

**Проблема:** Как предзаполнять форму в `QuestRewardsDialog`.

**Ответ (пользователь):** `PostVictoryDialog` открывается **предзаполненным** из `TaskInfo` (материи, растения, задания, босс, стихия); после «Принять» в нём применяется всё как сейчас + 2 стихии (31.4).

### 63. «Редактировать» при поражении

**Проблема:** Что открывать при «Редактировать» в окне наград за поражение.

**Ответ (пользователь):** Форму с чекбоксами номеров заданий (как текущий `DefeatScreen`), сохранение через `onBattleFinished`; «Принять» сразу открывает задания из `TaskInfo.defeatOpenQuests`.

### 64. Флаг режима окна наград

**Проблема:** Как ViewModel сообщает UI, какое окно наград показывать и для какого задания.

**Ответ (пользователь):** Добавить в `CampaignUiState` флаг `showQuestRewards` + режим `questRewardsMode` (VICTORY/DEFEAT) + `activeQuestNumber`; окно наград читает `TaskInfo` из `taskInfoByQuestNumber`.

---

## Y. Задача 31.6: окно наград за главу (20.08.2026)

### 65. Переход к следующей главе

**Проблема:** Сейчас `saveVictory` (в `onConfirmVictory`/`onQuestRewardsAccept`) вызывает `advanceChapter` — глава увеличивается сразу при победе. По правилам «обведите номер следующей главы» происходит после наград главы.

**Ответ (пользователь):** При «Принять» награды главы: текущая глава += 1, плюс применяются эффекты (кузня/лаборатория, задания). Для этого из `onQuestRewardsAccept`/`onConfirmVictory` убрать `advanceChapter` — переход к следующей главе станет частью наград главы.

### 66. Повышение уровней кузни/лаборатории

**Проблема:** Главы 4/8 содержат «Открывается 2/3 уровень лаборатории и кузницы» (`forgeUpgrade`/`labUpgrade`).

**Ответ (пользователь):** Повысить уровень на 1 при «Принять» (через новые методы репозитория `updateForgeLevel`/`updateLabLevel`).

### 67. Решения и условные сообщения глав

**Проблема:** Глава 7 требует интерактивного решения (диалог Да/Нет → достижение «Голос Волтьяра»); глава 4 — условное сообщение по достижению.

**Ответ (пользователь):** Реализовать решения и условные сообщения в рамках 31.6: решение гл.7 — диалог Да/Нет (достижение при выборе), условные сообщения показываются при наличии достижения.

---

## Z. Задачи 34.x: обмен, достижения, проверка условий (02.09.2026)

### 68. Удаление кнопки «Обмен»

**Проблема:** В листе кампании у каждого ресурса есть кнопка «Обмен», открывающая диалог обмена (продажа/обмен/получить у союзника — задача 33.7).

**Ответ (пользователь):** Удалить кнопку **и всю логику обмена**: `ExchangeDialog.kt`, методы/поля ViewModel, `enum ExchangeMode`. Репозиторные методы `getHuntersWithResource`/`exchangeResources` и `ResourceExchangeValidator`/`ExchangeResult` при этом **оставить** (переопределены в `FakeCampaignRepository` в commonTest; правки тестов запрещены) — при необходимости их удаление оформить отдельно в `testTasks.md`.

### 69. Статус достижений при редактировании

**Проблема:** Достижения отображаются зелёным (получено) / серым (не получено), но фактически сохраняются с `unlocked = false`.

**Ответ (пользователь):** Редактирование — только **добавление/удаление по названию**, без переключения статуса. Все достижения считаются «полученными» (сохранять с `unlocked = true`).

### 70. Награды при ручном завершении задания («Выполнено»)

**Проблема:** Кнопка «Выполнено» в листе кампании не проверяет условия задания (зависимые задания 4/6 не открываются).

**Ответ (пользователь):** При ручном завершении задания применять **только открытие заданий по условиям** (глава/достижения), без начисления материй/растений/достижений.

### 71. Составные условия (задание 25)

**Проблема:** У задания 25 условие составное («если есть достижение „Горящий уголёк“ → задание 27; если глава 8 → вместо 27 задание 34»). Текущая модель `TaskCondition` не поддерживает комбинацию «достижение + глава», seed хранит его как два независимых условия → неверный результат. Задания 29/40 содержат условные достижения («Голос Волтьяра» → «Уробборос»).

**Ответ (пользователь):** Расширить модель условий для поддержки составного условия («достижение И глава») и условных достижений.

### 72. Реализация расширенной модели условий (34.3)

**Решение (реализация):** `TaskCondition` расширен:
- новый вид `ACHIEVEMENT_OWNED_IN_CHAPTER` («достижение получено И глава ∈ chapterSet») — для зад. 25;
- поле `rewardAchievement: String?` — условие выдаёт достижение (зад. 29/40: «Голос Волтьяра» → «Уробборос»);
- формат строки условия дополнен 6-м необязательным сегментом `rewardAchievement` (обратная совместимость: парсер принимает 5–6 сегментов);
- при применении условий открывается первое условие, давшее целевое задание (зад. 25: глава 8 → задание 34 вместо 27);
- миграция `11→12` пере-сидит `task_info` (схема не менялась).


---

## AA. Задачи 35.x: «Устойчивость», начальная ярость, иконки ресурсов (03.09.2026)

### 73. Переименование «Затвердевший» → «Устойчивость»

**Проблема:** В правилах игры «Затвердевший» (статус монстра, с. 109) и «Устойчивость» (ключевое слово карты стойки, с. 116) — разные механики. В приложении статус монстра называется «Затвердевший».

**Ответ (пользователь):** Переименовать статус монстра в «Устойчивость». Заменить подписи в меню боя (`BattleScreen.kt`) и сообщение в `BattleViewModel.toggleHardened()` («Монстр затвердевший/незатвердевший» → «Монстр устойчивый/неустойчивый»). Поле `isHardened` и логику не менять.

### 74. Начальная ярость монстра

**Проблема:** Сейчас монстр создаётся с `rage = 0`.

**Ответ (пользователь):** В самом начале боя у монстра должно быть столько ярости, сколько игроков — в `BattleViewModel.startBattleWithHunters()` задать `rage = hunters.size`.

### 75. Иконки ресурсов — папка и имена

**Проблема:** В корне проекта лежат PNG иконки ресурсов с русскими именами в верхнем регистре (21 файл, включая «Селикорния»).

**Ответ (пользователь):** Перенести в `androidApp/src/main/res/drawable/` и переименовать в нижний регистр по именам enum (`scales.png`, `bones.png`, `nillea.png`, `fire.png`, … `selicornia.png`). Исходники из корня удалить. Без префикса `ic_`.

### 76. Иконки в меню компании — объём

**Проблема:** Меню компании реализовано только в Android (iOS — заглушка `ContentView.swift`).

**Ответ (пользователь):** Иконки добавлять только для Android (`CampaignSheetScreen.kt`), в iOS Assets не переносить.

---

---

## AB. Задачи 36.x: пролог — сразу награда главы (03.09.2026)

### 77. Какое окно скрыть в прологе

**Проблема:** В прологе после победы над Вираксеном (первый бой новой кампании) сейчас показывается окно `PostVictoryDialog` «Победа! / Задание выполнено!» (выбор босса, ресурсы за победу, чекбоксы открытых заданий 1–49). Окно «Награды за задание» (`QuestRewardsDialog`) в прологе и так не показывается.

**Ответ (пользователь):** Скрывать именно `PostVictoryDialog` «Задание выполнено!» в прологе.

### 78. Награды пролога при скрытии окна

**Проблема:** Окно «Задание выполнено!» сейчас отвечает за трофей Вираксена и начисление 2 стихий огня.

**Ответ (пользователь):** Трофей Вираксена сохранять и 2 стихии «Огонь» начислять каждому охотнику **молча, без показа окна**, сразу открывать окно наград главы.

### 79. Объём «и только для него»

**Проблема:** Нужно ли распространять пропуск окна на другие случаи.

**Ответ (пользователь):** Изменение действует только для самого первого боя новой кампании (пролог). Все последующие победы над заданиями сохраняют текущие окна наград.

### 80. Поражение в прологе

**Проблема:** `onDefeat` всегда открывает окно «Награды за поражение».

**Ответ (пользователь):** Не распространять изменение на поражение; поражение в прологе оставить как есть.

---

## AC. Задачи 37.x: подсветка параметров и вибрация в бою (06.09.2026)

### 81. Сценарии вибрации

**Проблема:** Текст запроса «нажатие любой клавиши / нанесение урона / рана» допускал разные трактовки.

**Ответ (пользователь):** Короткий виброотклик при нажатии любой кнопки и при применении урона (в т.ч. авточерез 2 секунды после быстрой кнопки). При нанесении раны — двойной отклик, который **заменяет** короткий.

### 82. Параметры для подсветки

**Проблема:** «здоровье, ярость, накопленный урон, раунд и т.п.» — неоднозначный перечень.

**Ответ (пользователь):** Подсвечивать **все** параметры боя: фаза, раунд, здоровье, ярость, накопленный урон, прочность, статус, смена стойки.

### 83. Тип вибрации и платформа

**Проблема:** Нужен выбор способа вибрации.

**Ответ (пользователь):** Android-реализация через `Vibrator` + разрешение `VIBRATE` (iOS — заглушка, не трогаем).

### 84. Вибрация при вводе текста

**Проблема:** Вибрировать ли на каждую клавишу в поле «Ввести урон».

**Ответ (пользователь):** Только кнопки/переключатели; на ввод символов в поле урона не вибрировать.

---


---

## AD. Задачи 38.x: иконка приложения (06.09.2026)

### 85. Платформы для иконки

**Проблема:** Куда добавлять иконку приложения.

**Ответ (пользователь):** Только Android (`androidApp`). iOS — заглушка без AppIcon, не трогаем.

### 86. Тип иконки

**Проблема:** Один растровый источник `icon.png`.

**Ответ (пользователь):** Legacy PNG в `mipmap-{mdpi,hdpi,xhdpi,xxhdpi,xxxhdpi}/ic_launcher.png` (масштаб из icon.png) + `android:icon="@mipmap/ic_launcher"` в AndroidManifest.

---

## AE. Задачи 39.x: переименование приложения (06.09.2026)

### 87. Объём переименования

**Проблема:** Где менять отображаемое имя приложения на «Companion PA».

**Ответ (пользователь):** Android — `app_name` в `strings.xml`; iOS — добавить `CFBundleDisplayName` = «Companion PA» в `Info.plist`.

### 88. Заголовок главного экрана

**Проблема:** Что делать с заголовком «Primal: Пробуждение» на главном экране.

**Ответ (пользователь):** Изменить на «Компаньон в игре „Primal: Пробуждение“» (Android `MainMenuScreen`, iOS `ContentView`).

---

## AF. Задачи 40.x: дефект предзаполнения стоек Вираксена в прологе (06.09.2026)

### 89. Предзаполнение стоек 2 и 3 Вираксена в прологе

**Проблема:** В прологе при смене стойки Вираксена данные 2 и 3 стойки не предзаполняются.

**Уточнение (пользователь):** Отсутствие предзаполнения проявляется, когда идёт смена стойки сразу с 1-й на 3-ю (пропуск промежуточной) — в диалоге нет данных для пропущенной стойки.

**Ответ (пользователь):** На данный момент изменения не требуются — оставить как есть. Задача на реализацию не создаётся.

---
